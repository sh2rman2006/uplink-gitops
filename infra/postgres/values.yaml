image:
  repository: postgres
  tag: "17"

postgres:
  user: uplink
  database: uplink
  passwordSecret: uplink-postgres-secret

persistence:
  enabled: true
  storageClass: local-path
  size: 10Gi

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
