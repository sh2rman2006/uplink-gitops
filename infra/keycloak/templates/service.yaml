apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: uplink-keycloak
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080
